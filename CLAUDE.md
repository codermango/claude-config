# Claude Code 全局配置

> 这是我的 Claude Code 全局配置文件，包含了我在所有项目中使用的通用指令和偏好设置。

## 通用偏好设置

### 语言偏好
- 永远使用中文回复
- 代码注释使用中文
- 文档和 README 使用中文编写

**特殊规则：工作项目**
- 如果当前工作目录在 `/Users/ke.ma/code/work/` 下：
  - 对话回复仍使用中文
  - 代码注释使用英文
  - 文档（README、API 文档等）使用英文
  - 变量、函数命名使用英文
  - Git 提交信息使用英文

### 代码风格
- 优先使用简洁、可维护的代码
- 避免过度工程化
- 只在必要时添加注释，代码应该自解释
- 使用有意义的变量和函数命名

### 工作流程
- 在修改代码前，先阅读相关文件
- 对于复杂任务，使用 TodoWrite 工具进行规划
- 遇到不确定的地方，使用 AskUserQuestion 工具询问
- 提交代码前，确保没有引入安全漏洞

### Git 操作规范
- **重要：永远不要自行执行 git commit 或 git push 操作**
- 只在用户明确要求时才执行 Git 提交和推送
- 完成代码修改后，告知用户需要提交，但不要主动提交
- 如果用户要求提交，使用约定式提交（Conventional Commits）格式
- 提交信息使用中文
- 格式：`<类型>: <描述>`
- 类型包括：
  - feat: 新功能
  - fix: 修复 bug
  - docs: 文档更新
  - style: 代码格式调整
  - refactor: 重构
  - test: 测试相关
  - chore: 构建/工具相关

### 文件引用格式
- 使用 Markdown 链接格式引用文件：`[文件名](路径)`
- 引用具体行号：`[文件名:行号](路径#L行号)`
- 引用行范围：`[文件名:起始-结束](路径#L起始-L结束)`

### 技术栈偏好
<!-- 根据你的实际技术栈调整 -->
- 前端：TypeScript + React + Vite
- 后端：Node.js + Express/Fastify
- 数据库：PostgreSQL/MongoDB
- 测试：Jest/Vitest
- 代码检查：ESLint + Prettier

### 项目结构偏好
```
project/
├── src/              # 源代码
│   ├── components/   # 组件
│   ├── utils/        # 工具函数
│   ├── types/        # 类型定义
│   └── tests/        # 测试文件
├── docs/             # 文档
├── scripts/          # 脚本
└── config/           # 配置文件
```

### 安全注意事项
- 避免命令注入、XSS、SQL 注入等常见漏洞
- 敏感信息使用环境变量
- 不要提交 .env、credentials.json 等敏感文件
- 在系统边界处验证输入（用户输入、外部 API）

### 其他偏好
- 优先使用已有的依赖和工具，避免引入新的依赖
- 删除未使用的代码，不要保留注释掉的代码
- 错误处理要合理，不要添加不必要的 try-catch
- 保持代码简单，三行相似代码好于过早的抽象

---

<!--
## 项目特定配置
如果某个项目需要特殊配置，可以在该项目的 .claude/CLAUDE.md 中覆盖这些设置
-->
